#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
int c, a = 0;
while (c < 1024)
{
if ((c % 3 == 0) || (c % 5 == 0))
}
{
a += c;
}
{
c++;
}
printf("%d\n", a);
return (0);
}
